description = "Run the browser-preview-reviewer role using the project agent definition."

prompt = """
Act as the browser-preview-reviewer for this repository.

Follow this role definition:
@{.gemini/agents/browser-preview-reviewer.md}

Use available project skills from .gemini/skills or .agents/skills when they match the task.

User request:
{{args}}
"""
